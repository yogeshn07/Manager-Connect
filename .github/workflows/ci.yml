name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  flutter-analyze:
    name: Flutter Analyze & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      # Flutter 3.41.7 bundles Dart 3.11.5, satisfying the pubspec constraints
      # (sdk >=3.9.0 <4.0.0, flutter >=3.38.0). Keep this pin in sync with pubspec.yaml.
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.7'
          channel: stable
          cache: true

      - name: Print SDK versions
        run: flutter --version

      - name: Install dependencies
        run: flutter pub get

      - name: Verify code generation is up to date
        run: |
          dart run build_runner build --delete-conflicting-outputs
          git diff --exit-code lib/

      - name: Analyze
        run: flutter analyze --no-fatal-infos

      - name: Run tests
        run: flutter test

  deno-check:
    name: Deno Type Check
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend/supabase/functions

    steps:
      - uses: actions/checkout@v4

      - uses: denoland/setup-deno@v1
        with:
          deno-version: v1.x

      - name: Type check shared infrastructure
        run: deno check _shared/*.ts
